# 집가는 버전 정리 - 2026-09-11

수업 종료 전 정리본.

1. 채팅 화면
   - 기존 `page-banner`, `simple-content-card` 디자인 재사용
   - 채팅 input / 전송 버튼을 기존 파란색 버튼 계열로 통일
   - 메시지 목록도 기존 색상/테두리 계열로 정리
   - `cid_05`, `uid_05` 유지

2. 마이페이지 암호변경
   - 회원정보 수정 화면 기준으로 form 폭 340px 통일
   - input 폭 180px 통일
   - 버튼은 회원정보 수정 화면과 같은 스타일 재사용
   - placeholder / 취소 초기화 기능 유지

3. 상단 메뉴
   - 메뉴 증가로 가로로 밀리지 않도록 간격, 글자크기, 좌우 padding 축소
   - 1100px / 850px / 700px 구간 반응형 보정 추가

4. CSS 정리
   - 실제 적용 디자인은 `src/styles/design.css`에 통합
   - 중복되고 미사용이던 `Chat.css`, `ChangePW.css` 제거

검증
- 수정 JSX의 괄호/중괄호 균형 확인 완료
- CSS 클래스 참조 확인 완료
- 이 실행환경에서는 npm 의존성 재설치가 완료되지 않아 Vite 실제 build는 미실행
